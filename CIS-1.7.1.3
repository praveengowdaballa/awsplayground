#!/bin/bash

TEST_NAME="CIS-1.7.1.3"
TEST_SUMMARY="Ensure remote login warning banner is configured properly"
TEST_SEVERITY="High"

#--------
# test scripting
#--------

[ -e /etc/issue.net ] || exit 0

egrep -q '(\\v|\\r|\\m|\\s)' /etc/issue.net
if [ $? -eq 0 ]
then
    logit2 severity=$TEST_SEVERITY name="$TEST_NAME" summary="$TEST_SUMMARY"
fi

