#!/bin/bash

TEST_NAME="CIS-4.1.2"
TEST_SUMMARY="Ensure auditd service is enabled"
TEST_SEVERITY="High"

#--------
# test scripting
#--------

/sbin/chkconfig auditd
if [ $? -ne 0 ]
then
    logit2 severity=$TEST_SEVERITY name="$TEST_NAME" summary="$TEST_SUMMARY"
fi

